# For local dev (without Docker):
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/kagabo?schema=public"
# For Docker: DATABASE_URL is set in docker-compose.yml, no need to set here.

JWT_SECRET="change-this-to-a-secure-random-string-in-production"
NEXT_PUBLIC_APP_NAME="KAGABO"
UPLOAD_DIR="./uploads"

# Docker-specific (optional overrides for docker-compose.yml):
# POSTGRES_USER=frigo
# POSTGRES_PASSWORD=frigoSecure2026
# POSTGRES_DB=frigo
# APP_PORT=3000
